# Dale
# 아두이노로 만드는 침입 탐지 시스템입니다.

## 침입 탐지 시스템

### 준비물
1. 아두이노 우노
2. 충전용 배터리
3. 전원버튼, 인터럽트 버튼 모듈
4. LCD 패널(I2C)
5. PIR 센서

### 아두이노 핀번호 계획
1. 충전용 배터리 : 각각 Vin, GND에 배치
2. 전원버튼, 인터럽트 버튼 모듈 : 전원버튼은 5V, GND에 배치, 인터럽트 버튼 모듈은 5V, GND, PD2(2번 핀)에 배치
3. LCD 패널 : SDA는 PC4(A4 핀), SCL은 PC5(
